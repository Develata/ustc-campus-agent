# Legacy planning archive

This directory preserves source planning artifacts migrated from `/opt/data/107-competition` for provenance.

These files may contain stale decisions from before repository initialization. When they conflict with current ADRs, `docs/plan/`, `docs/architecture/`, `docs/contracts/`, or `docs/acceptance/`, the current canonical docs win.
